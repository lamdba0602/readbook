<nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <a class="navbar-brand" href="/">百鸟书屋</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
              <li class="active">
                <%= link_to("Books", books_path) %>
              </li>
            </ul>            
            <ul class="nav navbar-nav navbar-right">
               <li>
                  <%= link_to baskets_path do  %>
                     漂流瓶 <i class="fa fa-shopping-cart"> </i> ( <%= current_basket.books.count %> )
                  <% end %>
               </li>
                <li>
                  <% if !current_user %>
                    <li><%= link_to("<i class='fa fa-registered' aria-hidden='true'></i> Sign Up".html_safe, new_user_registration_path) %></li>
                    <li><%= link_to("<i class='fa fa-sign-in' aria-hidden='true'></i> Login".html_safe, new_user_session_path) %></li>
                  <% else %>
                    <li class="dropdown">
                      <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                          Hi!, <%= current_user.email %>
                          <b class="caret"></b>
                      </a>
                      <ul class="dropdown-menu">
                        <% if current_user.admin? %>
                          <li>
                            <%= link_to("Admin 选单", admin_books_path ) %>
                          </li>
                        <% end %>
                        <li> <%= link_to("<i class='fa fa-sign-out' aria-hidden='true'></i> logout".html_safe, destroy_user_session_path, method: :delete) %> </li>
                      </ul>
                    </li>
                  <% end %>
                </li>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
